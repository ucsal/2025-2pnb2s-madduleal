package cms.user;

public class EditorUser extends User {
    public EditorUser(long id, String nome, String email, String password) {
        super(id, nome, email, password);
    }

    // métodos específicos de Editor (se houver) ficam aqui
}
